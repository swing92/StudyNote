Inversion of Control(IoC)
	> 제어역전 또는 제어역행으로 번역
	> 어떤 클래스에서 의존하는 객체를 그 클래스 내부에서 생성하지 않고 외부의 어떤 주체로 부터 전달 받는 
	> 제어순행 : 어떤 클래스에서 의존하는 객체를 new 연산자를 사용해 생성하는 것
				new 연산자를 사용해 객체를 생성하게 되면 객체를 생성한 클래스가 그 객체의 소유권을 가지고 객체를 제어할 수 있기 때문
	> 제어역행 : 외부의 주체로부터 객체를 주입받게 되면 객체를 사용할 수는 있지만 객체의 소유권을 가지고 있지 않은 경우